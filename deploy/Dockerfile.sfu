FROM node:18-alpine

WORKDIR /app

# 安装构建依赖
RUN apk add --no-cache python3 make g++

# 复制源码
COPY server/sfu/ .

# 安装 npm 依赖
RUN npm install

# 构建
RUN npm run build

# 暴露端口
EXPOSE 3000 40000-40100

# 启动
CMD ["node", "dist/index.js"]
